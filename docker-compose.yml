services:
  app:
    container_name: swd392-app
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8080}:8080"
    env_file:
      - .env
    environment:
      # JVM options
      JAVA_OPTS: "-Xms256m -Xmx512m"
